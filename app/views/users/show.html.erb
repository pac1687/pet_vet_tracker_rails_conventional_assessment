<div class="page-header">
  <h1>Pets</h1>
</div>
<div class="container">
  <ul>
    <% @pets.each do |pet| %>
      <li><h3><%= link_to "#{pet.name}", pet_path(pet) %></h3></li>
    <% end %>
  </ul>
</div>

<div class="page-header">
  <h2>New Pet</h2>
</div>

<div class="container">
  <%= render('pets/form') %>
</div>

<br>